language: node_js
node_js:
  - "9"
dist: trusty
#sudo: required
cache:
  directories:
    - node_modules
branches:
  only:
  - master
before_script:
  - npm install -g @angular/cli@7.0.6
script:
  - pushd front
  - ng build --prod
  - popd

deploy:
  provider: pages
  skip_cleanup: true
  keep_history: true
  github_token: $GITHUB_TOKEN
  local_dir: dist/ledwallapp
  on:
    branch: master
